@page
@model ProiectWeb.Pages.Clienti.CreateModel

@{
    ViewData["Title"] = "Adaugă client";
}

<h1>Creează</h1>

<h4>Client</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Contact.Nume" class="control-label"></label>
                <input asp-for="Contact.Nume" class="form-control" required/>
                <span asp-validation-for="Contact.Nume" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Contact.Nr_tel" class="control-label">Numar telefon</label>
                <input asp-for="Contact.Nr_tel" class="form-control" required type="number"/>
                <span asp-validation-for="Contact.Nr_tel" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Contact.Email" class="control-label"></label>
                <input asp-for="Contact.Email" class="form-control" required />
                <span asp-validation-for="Contact.Email" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Înapoi la listă</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
